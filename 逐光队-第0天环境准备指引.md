# 第 0 天 · 阿里云环境准备指引

> 适用：2026 年第二届重庆市 AI 大模型创新应用大赛 · 企业出题赛道（阿里「创意 AI 校园」）
> 负责：钟浩文（A）　复核：全队
> 目标：**今天之内**拿到 300 元算力 + 拿到能调通两个模型的 API Key

---

## 这两步到底在干什么

一句话：**把「赛题方给的资源」变成「你代码里能用的东西」。**

```
① 学生认证  →  领到 300 元算力券  →  这笔钱用于模型调用和部署
② 开通百炼  →  创建 API Key      →  这串字符是代码调用大模型的钥匙
```

两件事的共同点是：**都没做完就等于项目无法启动**（代码能写，但跑不起来）。所以放在第 0 天，且必须今天完成。

---

## 步骤 0.1 · 阿里云学生认证与 300 元算力领取

### 入口（赛题官方指定，别去首页瞎找）

```
https://university.aliyun.com/action/creative-ai
```

这是本次大赛阿里云赛题的**专属领券页**。赛题原文写的「阿里云为每位学生提供 300 元免费算力资源」就是从这里领，官方确认的领取方式是：**完成学生认证 → 自动发放**。

### 前置条件（不满足会白折腾）

| # | 条件 | 说明 |
|---|---|---|
| 1 | 已有阿里云账号并完成**个人实名认证** | 支付宝授权认证即可，秒过。**企业认证的账号不支持学生认证** |
| 2 | 中国境内高校**全日制**在校生 | 专科 / 本科 / 硕士 / 博士均可，**非全日制不支持** |
| 3 | 用你长期在用的账号 | ⚠️ 一个身份证号只能绑定一个阿里云账号，**认证成功后无法取消、无法换号** |

### 操作（PC 端，手机也行）

1. 浏览器打开上面的专属页，登录阿里云账号。
2. 找到「我是学生」区域，点 **立即申请 / 学生认证**。
3. 选择「国内学生认证」，用**与账号实名一致的那个支付宝**扫页面上的二维码。
4. 在支付宝里确认账号与实名信息 → 点同意 → 系统自动向学信网核验学籍。
5. 回到页面，300 元算力券随认证通过自动发放到账户卡包。

> 整个过程一般 1-5 分钟，不需要打印材料、签字或按手印。如果卡住，先换手机阿里云 App 走一遍（发现页 → 普惠权益 → 高校 → 完成学生认证）。

### 产出（两张截图 → 共享文件夹 `05-文档/截图/`）

1. 阿里云**账号中心**显示「学生验证：已验证」的页面；
2. 【费用与成本 → 卡券管理 → 优惠券管理】里能看到那张 300 元券。

### 验收标准

- [ ] 账号中心 → 我的账号，学生验证状态为 **已验证**
- [ ] 卡券管理里能看到 300 元券，且**有效期 1 年**
- [ ] 手机阿里云 App 登录同一账号，状态一致（排除浏览器缓存的错觉）

### 三个必须知道的坑

**坑 1 · 学生身份有效期只有 30 天。**
过期不影响已购资源，但会影响再次享受优惠。9 月 24 日提交流程内不用管；如果进了 10-11 月决赛，手机上再扫一次续期即可，1 分钟的事。

**坑 2 · 券不是现金，是抵扣券。**
只能在购买指定产品时抵扣，且「同一商品仅可抵扣 1 次」。所以别零散买小额资源，**攒着用在决赛部署上**（比如 ECS 或函数计算）。

**坑 3 · 如果这个账号以前领过 300 元学生券。**
规则是「每人每 12 个月可领取一张」。如果钟浩文之前已经领过，本次可能领不到第二张。真遇到就截图，连同参赛信息一起联系赛事 QQ 群 **1061821631** 说明情况。**不要反复注册新号** —— 新号过不了实名，而且一个人只能认证一个账号。

**另外：领到券 ≠ 百炼能用。** 百炼要在百炼控制台单独开通，见下一步。

---

## 步骤 0.2 · 开通百炼并拿到 API Key

### 入口

```
https://bailian.console.aliyun.com/
```

用**同一个**阿里云账号登录（认证过的那个）。

### 操作

1. 首次进入会提示**开通百炼服务**，勾选协议后点开通。免费开通，后续按量计费。
2. 左侧菜单进「**模型广场**」，确认下面两个模型对当前账号可用，需要单独点开通的就点一下：
   - `qwen-plus` —— 通义千问，负责生成回答
   - `text-embedding-v3` —— 负责把文本转成向量
3. 进「**API-KEY 管理**」（部分版本在右上角头像 → API-KEY）→「创建我的 API-KEY」→ 选账号 → 确定。
4. **立刻复制这个 `sk-` 开头的 Key。** 它只在创建时完整显示一次，弹窗一关就只能重建。
5. 把 Key 写进代码：

```bash
cd code
copy .env.example .env          # macOS / Linux 用 cp
# 用记事本打开 .env，把这行改成自己的 Key：
# DASHSCOPE_API_KEY=sk-你的真实Key
```

> `.env` **绝对不能提交到 GitHub**。仓库里已经放了 `.gitignore` 专门排除它。

### 产出

1. `code/.env` 文件（含真实 Key，只存在本地）；
2. Key 创建成功页截图 → `05-文档/截图/`（⚠️ **截图里不能露出完整 Key**，截到前几位即可）；
3. `code/` 下依赖安装完成。

### 验收：跑自检脚本

在 `code/` 目录下执行：

```bash
pip install -r requirements.txt
python check_env.py
```

**两个模型都打 `[OK]` 才算过关。**

期望输出（大致长这样）：

```
==============================================================
选课军师 · 环境自检（步骤 0.2 验收工具）
==============================================================
[OK] 配置读取成功
     API Key  ：sk-xxxxxx****abcd
     base_url ：https://dashscope.aliyuncs.com/compatible-mode/v1
     LLM      ：qwen-plus
     Embedding：text-embedding-v3
--------------------------------------------------------------
测试 1/2 · 对话模型 qwen-plus
[OK] 模型回复：通了
--------------------------------------------------------------
测试 2/2 · 向量模型 text-embedding-v3
[OK] 向量维度：1024
全部通过 — Key、对话模型、向量模型都可用。
```

### 为什么写成脚本而不是一行命令

网上常见的写法是 `python -c "…多行代码…"`。在 Windows 的 CMD 或 Git Bash 里，这种写法**引号和换行极易被 shell 吃掉**，报的错还看不懂，白白浪费半小时。

`check_env.py` 只跑一条命令，而且内置了报错翻译，会直接告诉你哪一步错了、下一步该点哪里。

### 排错表

| 报错关键字 | 真实原因 | 怎么办 |
|---|---|---|
| `AuthenticationError` / `401` / `invalid_api_key` | Key 抄错、复制时带了空格或换行 | 回百炼用「复制」按钮重新复制 |
| `model not exist` / `permission denied` | 模型没对当前账号开通 | 到「模型广场」点开通 |
| `Arrearage` / 欠费 / `quota` | 账户没有可用额度 | 查券是否到账、账户余额是否充足 |
| 连接超时 / `SSL` / `proxy` | 网络或代理拦截 | 关掉代理，确认能访问 `dashscope.aliyuncs.com` |

### 安全注意

- 别拿别人的 Key 做测试，也别把自己的 Key 发到群里或写进 GitHub。
- 阿里云明确规定：**学生券和学生认证账号禁止买卖、外借、出让**。账号出问题会连带影响全队的算力资源。

---

## 如果 0.2 今天卡住了怎么办

**不要为了等 Key 把整个项目停住。**

`ingest.py` 只做本地 PDF 解析和切片，**不需要联网、不需要 Key**。所以可以这样：

```bash
# 先把培养方案 PDF 放进 data/raw/，然后
python ingest.py
```

切片结果照样能产出，等 Key 到位再补向量化和检索验证。

**卡在 Key 上不等于今天没进度。**

---

## 今日完成清单

- [ ] 0.1 阿里云学生认证通过（截图已存 `05-文档/截图/`）
- [ ] 0.1 卡券管理中确认 300 元券到账，有效期 1 年
- [ ] 0.2 百炼开通，`qwen-plus` 与 `text-embedding-v3` 均可见
- [ ] 0.2 API Key 已创建并写入 `code/.env`
- [ ] 0.2 `python check_env.py` 两个模型都返回 `[OK]`
- [ ] 0.3 GitHub 仓库 + 共享文件夹建好，三人可访问
- [ ] 0.4 接口 JSON 格式已定稿并写进共享文档
- [ ] 今晚 21:30 站会：确认以上全部完成，任何未完成项当场定责任人和补做时间

---

## 卡住了找谁

| 问题类型 | 找谁 |
|---|---|
| 阿里云认证 / 领券 / 账号问题 | 阿里云客服 4008013260，或赛事 QQ 群 1061821631 |
| 赛题理解、组队规则 | 赛事 QQ 群 1061821631（进群备注：学校-姓名） |
| 代码跑不通 | 先看 `code/README.md` 的常见问题，再问钟浩文 |
| 进度落后 | 当晚站会提出，不要沉默延期 |
