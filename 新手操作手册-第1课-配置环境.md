# 新手操作手册 · 第 1 课

**目标：把「选课军师」的运行环境配置好，让代码能跑起来。**

这一课你只需要做一件事：**复制粘贴一条命令，然后粘贴一次你的 API Key。**

全程大约 5 分钟（第一次装依赖可能等 1-3 分钟）。

> 使用说明：本手册假设你完全没有命令行经验。每一步都会告诉你
> 「点哪里」「会看到什么」「接着做什么」。看到不懂的词不用管，照着做就行。

---

## ⚠️ 开始之前：必须先把泄露的 Key 删掉

你之前把包含完整 API Key 的截图发出来过。**那个 Key 已经算泄露，必须先删掉。**

操作：

1. 浏览器打开 `https://bailian.console.aliyun.com/`
2. 右上角**头像** → 点 **API-KEY 管理**
3. 找到你之前创建的那个 Key，点右边的**删除**
4. 点**创建我的 API-KEY**，**归属业务空间**选「默认业务空间」，**权限**选「全部」
5. 弹窗出现后，**立刻点「复制」按钮**

⚠️ Key 只显示这一次。**复制后先粘到本地记事本存着，但不要再截图、不要再发到任何群里。**

> 如果你已经删过了、也建好了新 Key，直接跳到第 1 步。

---

## 第 1 步：打开「命令提示符」黑窗口

**做法（选一个）：**

**方法一（推荐，最快）**
1. 按住键盘上的 `Win` 键（在 Ctrl 和 Alt 中间，有个窗户图标），再按字母 `R`
2. 屏幕左下角会弹出一个小窗口，里面有个输入框
3. 在输入框里打三个字母：`cmd`
4. 按回车键

**方法二**
1. 点屏幕左下角的**开始菜单**（窗户图标）
2. 直接打字：`cmd`
3. 看到「命令提示符」后点它

**你会看到：** 一个纯黑色的窗口，里面有几行白字，最后一行大概长这样：

```
C:\Users\zhong>
```

**这就对了。** 那个闪烁的光标在等你输入命令。后面这个 `>` 符号叫「提示符」，它左边显示的是你「现在站在电脑的哪个文件夹里」。这部分看不懂也没关系。

---

## 第 2 步：复制粘贴这一整条命令

**复制下面这一整行**（用鼠标从头拖到尾，选中，然后按 `Ctrl+C`）：

```
A:\Pyhton\python.exe "C:\Users\zhong\WorkBuddy\2026-09-17-16-19-59\逐光队-选课军师\code\setup.py"
```

然后**回到黑色窗口**，**粘贴**：

- 在黑色窗口的**空白处点一下鼠标右键** → 命令会自动粘贴进去
- 或者按 `Ctrl+V`（部分系统需要先右键点一下窗口标题栏）

**你会看到：** 黑窗口里出现了这行命令。

然后按**回车键**。

> 如果提示「不是内部或外部命令」或者「系统找不到指定的路径」，
> 说明路径打错了。请回到上面重新完整复制一次，注意要有开头的 `A:\Pyhton\` 和
> 结尾的 `setup.py"`。

---

## 第 3 步：它会自动装依赖（等待 1-3 分钟）

**你会看到：**

```
================================================================
        选课军师 · 一键环境配置
        逐光队 · 2026 重庆市 AI 大模型大赛
================================================================
项目位置：C:\Users\zhong\...\code

================================================================
[1/5] 检查 Python 版本
================================================================
当前 Python：3.13.7
所在位置  ：A:\Pyhton\python.exe

[OK] Python 版本符合要求。

================================================================
[2/5] 检查依赖库
================================================================
  [已有] openai
  [缺失] python-dotenv
  ...
缺少 5 个库，需要安装：...
安装过程需要联网，大约 1-3 分钟，会滚动很多行英文——那是正常的。

现在就自动安装吗？ [直接回车 = 是]：
```

**你要做的：** 直接按**回车键**。

**然后会滚动很多行英文。** 那是正常的安装日志，不用看懂，也不用管红字。
**等待 1-3 分钟**，直到它停下来。

> 如果卡了很久没动静，检查一下网络。安装是从清华镜像下载的，国内速度很快。

---

## 第 4 步：粘贴你的 API Key

**你会看到：**

```
================================================================
[4/5] 配置 API Key
================================================================

请按下面两步操作：
  第 1 步：打开浏览器进入百炼控制台
           https://bailian.console.aliyun.com/
  第 2 步：右上角头像 → API-KEY 管理 → 创建我的 API-KEY
           创建后点「复制」按钮

把 Key 粘贴到这里，然后按回车：
```

**你要做的：**

1. 如果你手上已经有新 Key（刚才复制过的），直接**右键粘贴**到这个黑窗口里
2. 按**回车键**

**如果你还没创建 Key：** 先别关这个窗口，去浏览器按上面提示建一个、复制好，再回来粘贴。

**粘贴后你会看到：**

```
[OK] 已写入配置文件：.env
     使用的 Key：sk-abc1234********wxyz
```

（中间那串星号是保护措施，不会把你的完整 Key 显示出来。）

接着它会问你是否要填自定义地址：

```
要现在把这个地址也填进去吗？（不知道就回车跳过）
```

**如果你在百炼控制台看到过 `ws-` 开头的一长串地址**，就把那个地址粘贴进去、按回车。
**如果不确定**，直接按回车跳过（默认地址通常能用）。

---

## 第 5 步：确认成功

**它会问你：** `开始验证吗？ [直接回车 = 是]` —— 按**回车**。

**然后你会看到：**

```
[OK] 配置读取成功
     API Key  ：sk-abc1234********wxyz
     base_url ：https://...
     LLM      ：qwen-plus
     Embedding：text-embedding-v3
--------------------------------------------------------------
测试 1/2 · 对话模型 qwen-plus
[OK] 模型回复：通了
--------------------------------------------------------------
测试 2/2 · 向量模型 text-embedding-v3
[OK] 向量维度：1024
==============================================================
全部通过 — Key、对话模型、向量模型都可用。
步骤 0.2 完成，可以开始第 1 天的数据准备。
==============================================================
```

**看到「全部通过」四个字，这一课就完成了。**

最后窗口会停在 `按回车键结束...`，按一下回车就行。

---

## 出错了怎么办

**对照下表找到你的情况。** 找不到也没关系，把黑窗口里的内容截图（记得给 Key 打码）发给我。

| 你看到的 | 原因 | 怎么办 |
|---|---|---|
| `'A:\Pyhton\python.exe' 不是内部或外部命令` | 路径复制不完整 | 回到第 2 步，重新完整复制那条命令 |
| `系统找不到指定的路径` | 命令被浏览器或输入法改动了 | 手动检查有没有多余空格，重新复制粘贴 |
| `[X] 对话模型调用失败：AuthenticationError` | Key 复制不完整或已失效 | 重新去百炼创建 Key，用「复制」按钮，再运行一次 setup.py |
| `[X] 模型未开通` | qwen-plus 没对账号开通 | 百炼控制台 → 模型广场 → 搜索 qwen-plus → 点开通 |
| `[X] 额度不足` | 免费额度用完或没到账 | 查阿里云「费用中心 → 卡券管理」，确认 300 元券是否到账 |
| 连接超时 / SSL 错误 | 网络或代理问题 | 关掉 VPN / 代理再试；确认能打开 bailian.console.aliyun.com |
| 装依赖时一堆红字 | 网络波动 | 重新运行一次 setup.py，它会跳过已装好的库继续 |

**如果报错里有 `[X]`，脚本会自己给出下一步建议** —— 先照着它说的做。

---

## 这一课完成后，你的电脑上会多出什么

```
逐光队-选课军师\
├── code\                    ← 代码文件夹
│   ├── .env                 ← 新增：存你的 Key（只在本地，不要外传）
│   ├── setup.py             ← 新增：一键配置脚本
│   ├── check_env.py         ← 环境自检脚本
│   ├── data\raw\            ← 放培养方案 PDF 的地方（下一课用）
│   └── ...其他代码文件
```

---

## 下一课预告

环境配好后，下一步是**把培养方案放进系统**：

1. 找到重庆邮电大学的本科培养方案 PDF 文件
2. 复制到 `code\data\raw\` 文件夹里
3. 运行 `ingest.py`，让它把 PDF 拆成可检索的知识片段

**需要提前准备的：** 一份重庆邮电大学的培养方案 PDF（去学校教务处官网找，
或者问班学长要）。没有它，后面的检索问答做不了。

---

## 一句话总结这一课

> 打开黑窗口 → 粘贴一条命令 → 回车 → 粘贴 Key → 回车 → 看到「全部通过」。

就这些。
