# 新手操作手册 · 第 2 课 · 建仓库与共享目录（步骤 0.3）

> 适用对象：编程/命令行零基础的队员
> 本课完成的事：
> - A（钟浩文）：建好 GitHub 仓库，把本地代码推到网上
> - C（古雯茜）：建好腾讯文档共享文件夹，把现有 5 份文档上传进去
> - 验收：三人能在浏览器看到仓库代码、能编辑腾讯文档

---

## 开始前必读（30 秒）

- 全程你只需要做三件事：**点鼠标**、**粘贴命令**、**等几秒**
- 凡是带 ⭐ 的地方是要你**截图发群里**的关键节点
- 凡是看到「`>` 开头的灰色字」是要你在命令行里执行的内容，**整段复制粘贴**就行
- 命令复制后回车，看到新一行的 `C:\Users\zhong>` 就说明执行完了

---

# 🅰️ A 部分：建 GitHub 仓库并推送代码

> 总耗时预计 15-20 分钟

## A-1. 注册 / 登录 GitHub（5 分钟）

1. 打开浏览器，地址栏输入：`https://github.com/`
2. 如果已经注册过：右上角点 **Sign in** → 输入账号密码登录
3. 如果没注册过：点 **Create an account** → 按提示填：
   - 邮箱（建议用 QQ 邮箱）
   - 密码（至少 15 位，或 8 位 + 数字字母）
   - 用户名（**记住这个**，后面要用）

> 💡 GitHub 经常要求邮箱验证。如果收不到验证邮件，去垃圾箱找找；QQ 邮箱需要在「设置 → 反垃圾」里把 GitHub 加入白名单。

⭐ **完成后截图**：登录后的 GitHub 首页（左上角能看到你的头像）

## A-2. 建仓库（3 分钟）

1. 登录后，浏览器直接打开：`https://github.com/new`
2. 页面会显示一个表单，按下面填：

| 表单项 | 填什么 |
|---|---|
| **Repository name**（必填） | `xuanke-junshi` |
| **Description**（可选，建议写） | `选课军师 · 2026年第二届重庆市AI大模型创新应用大赛 · 阿里创意AI校园赛道` |
| **Public / Private** | 选 **Public**（公开） |
| ☐ Add a README file | ✅ 勾上 |
| ☐ Add .gitignore | 下拉选 **Python** |
| ☐ Choose a license | 下拉选 **MIT License** |

3. 点最下面绿色按钮 **Create repository**
4. 创建后，浏览器会跳到新仓库的页面。**把浏览器地址栏的 URL 完整复制下来发给我**（AI 预测并拼命令要用）。

⭐ **完成后截图**：新仓库的页面（能看到文件名 `README.md`、`LICENSE`）

## A-3. 安装 GitHub 身份认证（首次推送前必做，5 分钟）

GitHub 现在不再允许直接用账号密码推送，必须用 **Personal Access Token (PAT)** 替代密码。

### 创建 Token

1. 登录状态下打开：`https://github.com/settings/tokens`
2. 点右上角 **Generate new token** → **Generate new token (classic)**
3. 表格填写：
   - **Note**：`xuanke-junshi`（写个备注，记住它是干嘛的）
   - **Expiration**：下拉选 **No expiration**（永不过期，方便）或 **30 days**
   - **Select scopes**：勾选 **`repo`**（全权访问仓库）和 **`workflow`**（可选）
4. 拉到页面底部，点绿色 **Generate token** 按钮
5. **关键步骤**：页面会显示一串字符（类似 `ghp_xxxxxxxxxxxxxxxxxxxx`），**立刻复制下来保存到记事本**，**关闭页面就再也看不到第二次了**

⭐ **完成后截图**：生成的 token（**只截前几位和后几位**就行，不要截完整的）

> ⚠️ **这个 token 等同于密码**，绝对不要发群、不要截图完整、不要提交到仓库。我会帮你写进 `.gitignore`，不会被上传。

## A-4. 推送本地代码到 GitHub（5 分钟）

### 第一次推送的命令

回到你要执行命令的地方。

### 打开 CMD 的两种方法

**方法一**：按 `Win + R` 键，输入 `cmd` 回车（黑色窗口就是 CMD）

**方法二**：在任务栏搜索框输入「cmd」回车

### 执行命令

打开 CMD 后，**整段复制下面这段命令**，粘到 CMD 里，按回车：

```
cd /d "C:\Users\zhong\WorkBuddy\2026-09-17-16-19-59\逐光队-选课军师" && git init && git add . && git commit -m "init: 比赛选题+方案书+代码骨架" && git branch -M main && git remote add origin https://github.com/你的用户名/xuanke-junshi.git && git push -u origin main
```

**执行前**，把命令里的 `你的用户名` 替换成你在 A-1 注册时用的 GitHub 用户名。

执行过程中：

1. 第一次 `git push` 时会弹一个登录窗口：
   - **Username**：填 GitHub 用户名
   - **Password**：填 A-3 生成的 **token**（不是账号密码）
2. 成功后会看到一堆 `Writing objects: 100%` 字样
3. 最后一行是 `Branch 'main' set up to track remote branch 'main'`

⭐ **完成后截图**：CMD 最后几行 + GitHub 仓库刷新后的页面（应该能看到所有文件名了）

### 命令逐段解释（看不看都行）

| 段 | 作用 |
|---|---|
| `cd /d "..."` | 进入项目目录 |
| `git init` | 把这个文件夹变成 git 仓库（本地） |
| `git add .` | 把所有文件加到待提交列表 |
| `git commit -m "..."` | 提交（保存）这次改动 |
| `git branch -M main` | 把分支改名为 main（GitHub 默认是 main） |
| `git remote add origin ...` | 关联远程 GitHub 仓库 |
| `git push -u origin main` | 推送到 GitHub |

---

## A-5. 验证是否成功（1 分钟）

刷新 GitHub 仓库页面（按 `Ctrl + F5` 强制刷新），应该能看到：

- `README.md`
- `code/` 文件夹（里面有 10 个 .py 文件）
- 5 个 .md / 6 个 .docx 文件
- `.gitignore`（自动生成）
- `LICENSE`

如果都看到了，就成功了。

---

## A-6. 检查 Key 是否被上传（关键！）

GitHub 推送**默认会排除** `code/.env`，因为我们写了 `.gitignore`。

但保险起见，去你的仓库页面看看：

- 打开 `https://github.com/你的用户名/xuanke-junshi`
- 找到 `code/` 文件夹
- 如果里面有 `.env`，**立刻告诉我**！要立刻删除（删除方法见 A-7）
- 如果看不到 `.env`，说明 Key 没泄露 ✅

## A-7. 如果 .env 不慎被上传了（紧急处理）

1. 打开 `https://github.com/你的用户名/xuanke-junshi/code/.env`
2. 如果能看到你的 API Key，立刻做下面两件事：
   - **回到百炼控制台删除这个 Key**（最关键）
   - **新建一个 Key**，跑 `setup.py` 第 4 步写入
3. 在仓库页面按 `Del` 键删除这个文件（需要 GitHub 网页编辑器操作），不会的话直接告诉我，我给你命令

---

# 🅲 C 部分：建腾讯文档共享文件夹

> 古雯茜执行，钟浩文只需要验收
> 总耗时预计 10 分钟

## C-1. 注册 / 登录腾讯文档

1. 打开：`https://docs.qq.com/desktop/`
2. 用微信扫码登录（最方便）
3. 登录后能看到「个人文件」页面

## C-2. 建顶级文件夹

1. 在「个人文件」页面左上角，点 **+ 新建** → **文件夹**
2. 命名为：**逐光队-选课军师**（**不要带任何特殊符号**，横线用半角）
3. 双击进入这个文件夹

## C-3. 建 7 个子文件夹

在新文件夹里点 **+ 新建 → 文件夹**，按下面顺序建（一个一个建）：

```
01-数据原始
02-数据清洗
03-代码
04-评测
05-文档
06-演示
07-提交
```

然后进入 `04-评测` 文件夹，再建 2 个子文件夹：

```
题库
结果
```

## C-4. 把现有 5 份文档上传到 05-文档

进 `05-文档` 文件夹，再建 4 个子文件夹：

```
参赛方案
分工
执行
新手操作
```

### 上传文件

> 文件都在钟浩文的电脑里，需要钟浩文先把文件发给古雯茜（微信文件传输 / 邮箱都行）

钟浩文电脑上的文件路径：

```
C:\Users\zhong\WorkBuddy\2026-09-17-16-19-59\逐光队-选课军师\
```

需要上传的 5 份文档：

| 类别 | 文件名 | 数量 |
|---|---|---|
| 参赛方案 | `选课军师-参赛方案书.md` + `.docx` | 2 |
| 分工 | `逐光队-八天分工方案.md` + `.docx` | 2 |
| 执行 | `逐光队-逐日执行手册.md` + `.docx`、`逐光队-第0天环境准备指引.md` + `.docx` | 4 |
| 新手操作 | `新手操作手册-第1课-配置环境.md` + `.docx` | 2 |

总共 **10 个文件**，每个分类对应的文件夹内：

- `参赛方案/` 放「参赛方案」的 md + docx（2 个）
- `分工/` 放「分工」的 md + docx（2 个）
- `执行/` 放「执行」的 4 个文件（执行手册×2 + 第0天指引×2）
- `新手操作/` 放「新手操作」的 2 个

### 上传方法

- 在腾讯文档文件夹里点 **上传** → 选「文件」
- 一次选多个文件上传
- 等待上传完成（10MB 宽带大约 30 秒）

⭐ **完成后截图**：每个子文件夹里的文件列表

## C-5. 共享给钟浩文和周婧妍

1. 在「逐光队-选课军师」文件夹上点右键 → **分享**
2. 把「**获得链接的人可编辑**」打开
3. **复制链接**，发到赛事 QQ 群 **1061821631** 里（备注 学校-姓名）

⭐ **完成后截图**：分享链接 + 权限设置

---

# ✅ 验收清单（站会前 21:30 核对）

- [ ] 钟浩文：GitHub 仓库能打开 → 看到 `code/` 和 5 份文档
- [ ] 钟浩文：**确认 `.env` 没被上传**
- [ ] 古雯茜：腾讯文档链接在群里
- [ ] 古雯茜：10 个文件已上传到 05-文档/
- [ ] 周婧妍：能编辑腾讯文档、能访问 GitHub 仓库

---

# 🆘 报错对照表（万一出问题）

| 看到的报错 | 怎么办 |
|---|---|
| `git: command not found` | Git 没装好，重装 `https://git-scm.com/download/win` |
| `Permission denied (publickey)` | 没配 SSH，先用 HTTPS 方式（按 A-4 的方法） |
| `fatal: not a git repository` | 没在项目目录里运行，先 `cd /d "..."` |
| `Updates were rejected because the remote contains work` | 仓库里有 README，先 `git pull origin main` 再 push |
| `could not find UI helper` | Windows 弹窗被拦截，看任务栏有没有小盾牌图标 |
| 腾讯文档上传超时 | 文件太大；先压缩 zip 再上传；或一次少传几个 |

---

# 💡 进阶（时间充裕才做）

- 建 Gitee 镜像（国内访问快）：把 A-2 的 `github.com` 替换成 `gitee.com`，命令末尾的 `github.com` 也换 `gitee.com`，再跑一次
- 把 token 存进 Windows 凭据管理器，避免每次输入：
  ```
  git config --global credential.helper manager
  ```