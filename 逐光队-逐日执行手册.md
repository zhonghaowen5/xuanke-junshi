# 逐光队 · 逐日执行手册

## 使用说明

本手册把八天计划拆成可执行的具体步骤。每一步都包含五项信息：

| 项 | 含义 |
|---|---|
| 负责 | 谁来做（A=钟浩文 / B=周婧妍 / C=古雯茜 / 全=全员） |
| 操作 | 具体做什么 |
| 命令 | 需要敲的命令或需要点的按钮 |
| 产出 | 做完之后产生什么文件 |
| 验收 | 怎么判断这一步真的做对了 |

**执行原则：** 按天推进，当天的步骤全部完成才进入下一天。任何一步的「验收」不通过，当晚站会必须提出。

---

## 第 0 天 · 9 月 17 日（今天）

### 步骤 0.1 · 阿里云学生认证与算力领取

- **负责：** A（钟浩文）
- **入口（赛题官方指定，别去首页瞎找）：** `https://university.aliyun.com/action/creative-ai`
  这是本次大赛阿里云赛题的专属领券页。赛题原文写的「阿里云为每位学生提供 300 元免费算力资源」就是从这里领，确认路径是「完成学生认证 → 自动发放」。

**前置条件（不满足会白折腾）**

1. 已有阿里云账号，且完成**个人实名认证**（支付宝授权，秒过）。**企业认证的账号不支持学生认证。**
2. 身份是中国境内高校**全日制**在校生（专科/本科/硕士/博士）。**非全日制不支持。**
3. 一个身份证号只能绑定一个阿里云账号，**认证成功后无法取消、无法换号**。用你自己长期在用的号，别拿临时号去做。

**操作（PC 端）**

1. 浏览器打开上面的专属页，登录阿里云账号。
2. 点「我是学生」区域的 **立即申请 / 学生认证**。
3. 选择「国内学生认证」，用**与账号实名一致的那个支付宝**扫页面上的二维码。
4. 在支付宝里确认账号与实名信息 → 点同意 → 系统自动向学信网核验学籍。
5. 回到页面，300 元算力券随认证通过自动发放到账户。

**产出（两张截图 → `05-文档/截图/`）**

1. 账号中心显示「学生验证：已验证」的页面；
2. 【费用与成本 → 卡券管理 → 优惠券管理】里能看到那张 300 元券。

**验收**

- 阿里云账号中心 → 我的账号，学生验证状态为**已验证**；
- 卡券管理里能看到 300 元券，且**有效期 1 年**；
- 手机阿里云 App 登录同一账号，状态一致（说明不是浏览器缓存骗你）。

**三个必须知道的坑**

1. **学生身份有效期只有 30 天。** 过期不影响已购资源，但会影响再次享受优惠。9 月 24 日提交流程内不用管；如果进了 10-11 月决赛，手机上再扫一次续期即可，1 分钟。
2. **券不是现金，是抵扣券。** 只能在购买指定产品时抵扣，且「同一商品仅可抵扣 1 次」。所以别零散买小额资源，攒着用在决赛部署上。
3. **如果这个账号以前领过 300 元学生券**（规则是每人每 12 个月一张），本次可能领不到。真遇到就截图联系赛事 QQ 群 1061821631 说明参赛情况，别自己反复注册新号——新号过不了实名。
4. **领到券 ≠ 百炼能用。** 百炼要在控制台单独开通，见步骤 0.2。

---

### 步骤 0.2 · 开通百炼并拿到 API Key

- **负责：** A
- **入口：** `https://bailian.console.aliyun.com/`（用同一个阿里云账号登录）

**操作**

1. 首次进入会提示**开通百炼服务**，勾选协议后点开通（免费开通，按量计费）。
2. 左侧菜单进「模型广场」，确认这两个模型对当前账号可用，需要单独开通的就点开通：
   - `qwen-plus`（通义千问，负责生成）
   - `text-embedding-v3`（负责向量化）
3. 进「API-KEY 管理」（部分版本在右上角头像 → API-KEY）→「创建我的 API-KEY」→ 选账号 → 确定。
4. **立刻复制这个 `sk-` 开头的 Key。** 它只在创建时完整显示一次，弹窗一关就只能重建。
5. 把 Key 落到代码里：
   - 进入 `code/` 目录，把 `.env.example` 复制一份改名为 `.env`；
   - 用记事本打开，把这行改成自己的 Key：`DASHSCOPE_API_KEY=sk-你的真实Key`；
   - `.env` **绝对不能提交到 GitHub**（仓库里已有 `.gitignore` 排除它）。

**产出**

1. `code/.env` 文件（含真实 Key，仅本地）；
2. Key 创建成功页截图 → `05-文档/截图/`（**截图里不能露出完整 Key**，只截到前几位即可）；
3. `code/` 下已安装依赖。

**验收：** 在 `code/` 目录下运行自检脚本，两个模型都打 `[OK]`：

```bash
cd code
pip install -r requirements.txt
python check_env.py
```

期望输出（大致）：

```
[OK] 配置读取成功
     API Key  ：sk-xxxxxx****abcd
     base_url ：https://dashscope.aliyuncs.com/compatible-mode/v1
     LLM      ：qwen-plus
     Embedding：text-embedding-v3
--------------------------------------------------------------
测试 1/2 · 对话模型 qwen-plus
[OK] 模型回复：通了
--------------------------------------------------------------
测试 2/2 · 向量模型 text-embedding-v3
[OK] 向量维度：1024
全部通过 — Key、对话模型、向量模型都可用。
```

**为什么要写成脚本而不是一行命令：** 在 Windows 的 CMD/Git Bash 里用 `python -c "..."` 跑多行代码，引号和换行极易被 shell 吃掉，报的错还看不懂。写成 `check_env.py` 只跑一条命令，报错信息也处理过，能直接告诉你哪一步错了。

**排错表**

| 报错关键字 | 真实原因 | 怎么办 |
|---|---|---|
| `AuthenticationError` / 401 | Key 抄错、复制时多了空格或换行 | 回百炼重新建一个，用「复制」按钮 |
| `model not exist` / permission denied | 模型没对账号开通 | 模型广场里点开通 |
| `Arrearage` / 欠费 / quota | 账户没额度 | 查券是否到账、账户余额 |
| 连接超时 / SSL | 网络或代理拦截 | 关掉代理，确认能访问 `dashscope.aliyuncs.com` |

**注意：** 别拿别人的 Key 做测试，也别把自己的 Key 发到群里或写进 GitHub。阿里云明确规定学生券和学生认证账号禁止买卖外借，账号出问题会连带影响全队的资源。

**⚠️ 如果 0.2 今天卡住了怎么办：** 不要为了等 Key 把整个项目停住。先把 `ingest.py` 跑起来（`python ingest.py` 只做本地 PDF 解析和切片，不需要联网），切片结果照样能产出，等 Key 到位再补向量化。**卡在 Key 上不等于今天没进度。**

### 步骤 0.3 · 建仓库与共享目录

- **负责：** A 建代码仓库，C（古雯茜）建文档共享文件夹
- **操作：**
  1. GitHub 建一个公开仓库，命名 `xuanke-junshi`；
  2. 共享网盘建七个目录，按分工方案里的结构；
  3. 把本手册、方案书、分工方案放进 `05-文档/`。
- **产出：** 仓库地址 + 共享文件夹地址。
- **验收：** 三个人都能访问仓库、都能编辑共享文件夹。

### 步骤 0.4 · 接口格式会议（今天最重要的 30 分钟）

- **负责：** 全（A 主讲）
- **操作：** 三人一起确定后端接口的返回结构，**当场写进共享文档**，之后不再改。
- **产出：** `05-文档/接口约定.md`
- **验收：** 文档里明确写死下面这个结构，B 和 A 都确认过。

```json
// POST /api/ask  请求
{ "question": "软件工程专业毕业最低学分是多少" }

// 响应
{
  "intent": "search_regulation",
  "answer": "软件工程专业毕业最低总学分为 170 学分。",
  "rejected": false,
  "cites": [
    { "n": 1, "source": "2024级本科培养方案.pdf",
      "chapter": "第三章 学分要求", "page": 12 }
  ]
}
```

**为什么这一步必须在今天做：** B 从 9 月 19 日起就能用假数据开发界面，不必等 A 的后端。这是三个人不互相卡住的关键。

---

## 第 1 天 · 9 月 18 日（报名截止日）

### 步骤 1.1 · 完成系统报名【最高优先级】

- **负责：** A 主操作，全确认
- **操作：** 登录大赛系统 `https://5cqs3u4l.mh.chaoxing.com/`，按提示提交「逐光队」报名信息，赛道选择「企业出题赛道 → 阿里出题：创意 AI 校园」。
- **产出：** 报名成功截图。
- **验收：** 系统显示报名已提交；**务必确认资格审核状态**，审核不通过无法提交作品。
- **注意：** 报名系统关闭后无法补报，这一天不做任何其他事情也要先把它完成。

### 步骤 1.2 · 采集培养方案数据

- **负责：** C（古雯茜）
- **操作：**
  1. 去学校教务处官网，下载目标专业的本科培养方案 PDF；
  2. 至少覆盖 1 个专业的全部年级；有条件就多下几个专业；
  3. 同时收集课程库、开课计划、学分修读要求；
  4. 每份文件重命名为「年份-专业-文件名.pdf」，放进 `01-数据原始/`。
- **产出：** `01-数据原始/` 下的 PDF 集合 + 一张数据清单表（文件名 / 来源链接 / 版本日期 / 页数）。
- **验收：** 随机翻 3 份 PDF，内容完整可读，没有加密或扫描件（扫描件需先做 OCR）。

### 步骤 1.3 · 搭建开发环境

- **负责：** A
- **操作：** 把代码骨架克隆下来，建虚拟环境，装依赖。

```bash
cd 逐光队-选课军师/code
python -m venv .venv
.venv\Scripts\activate          # Windows
pip install -r requirements.txt
copy .env.example .env          # 然后编辑 .env 填入 API Key
```

- **产出：** 可运行的本地环境。
- **验收：** `python -c "import openai, pandas, numpy; print('ok')"` 不报错。

### 步骤 1.4 · 前端设计稿

- **负责：** B（周婧妍）
- **操作：** 按接口约定文档，画出四个页签的界面：方案问答 / 学分诊断 / 选课建议 / 关于。重点是「引用出处」怎么展示 —— 建议做成可展开的折叠块。
- **产出：** 设计稿（Figma / 手绘拍照 / 直接写 Streamlit 布局都行）。
- **验收：** A 和 C 看过之后，能明确说出「用户点哪里、看到什么」。

---

## 第 2 天 · 9 月 19 日

### 步骤 2.1 · 数据清洗

- **负责：** C
- **操作：** 用代码骨架里的解析逻辑跑一遍所有 PDF，把正文和表格转成文本；对核心章节（学分要求、课程设置）逐页人工校对。
- **命令：**

```bash
PYTHONIOENCODING=utf-8 python ingest.py
```

- **产出：** `data/chunks/chunks.jsonl`
- **验收：** 抽 5 个片段检查，能看出「章节标题 + 完整条款 + 正确页码」，表格没有串行。
- **不通过怎么办：** 在 `ingest.py` 的 `HEADING_PATTERNS` 里补充本校培养方案的标题正则。

### 步骤 2.2 · 向量化建索引

- **负责：** A（C 交付数据后立即开始）
- **命令：**

```bash
python vectorstore.py build
```

- **产出：** `data/index/vectors.npz` + `meta.jsonl`
- **验收：** 索引文件生成成功，片段数量与 `chunks.jsonl` 一致。

### 步骤 2.3 · 验证检索质量【关键步骤】

- **负责：** A
- **操作：** 拿 3 个「正常问题」和 2 个「超纲问题」分别测检索得分，观察能否分开。

```bash
python vectorstore.py "软件工程专业毕业最低学分是多少"
python vectorstore.py "临床医学专业的培养方案是什么"
```

- **产出：** 一张得分记录表。
- **验收：** 正常问题最高分明显高于超纲问题（理想情况正常 ≥ 0.45、超纲 ≤ 0.30）。如果分不开，先调切片粒度，再调混合检索的权重（`vectorstore.py` 里的 `0.7 * vec + 0.3 * kw`）。
- **这一步是整个项目的地基。** 地基不稳，后面的引用溯源和拒答都做不好。

### 步骤 2.4 · 前端骨架

- **负责：** B
- **操作：** 用假数据把 `app.py` 的四个页签搭起来，能切换、能输入、能展示假结果。
- **产出：** 可点击的静态 Demo。
- **验收：** `streamlit run app.py` 能打开，四个页签都能点击，不报错。

---

## 第 3 天 · 9 月 20 日

### 步骤 3.1 · RAG 问答链路

- **负责：** A
- **操作：** 跑通「检索 → 拼 Prompt → 调模型 → 输出答案」全链路。

```bash
python rag.py "软件工程专业毕业最低学分是多少"
```

- **产出：** 能返回带引用答案的命令行工具。
- **验收：** 回答末尾能列出「文件名 · 章节 · 页码」三项出处。

### 步骤 3.2 · 标定拒答阈值

- **负责：** A
- **操作：** 用步骤 2.3 的得分记录，把 `config.py` 里的 `SCORE_THRESHOLD` 调到能把正常问题和超纲问题分开的位置。
- **验收：** 拿 10 个正常问题测，误拒不超过 1 个；拿 10 个超纲问题测，拒答不少于 8 个。
- **说明：** 这一步做扎实，实验二的拒答率基本就稳了。

### 步骤 3.3 · 验证后验校验机制

- **负责：** A
- **操作：** 运行 `verify.py` 自带的自检，确认「编造的数字会被拦截」。

```bash
PYTHONIOENCODING=utf-8 python verify.py
# 期望输出：170 学分 → 通过；185 学分 → 拦截
```

- **验收：** 能复现「正确放行、编造拦截」两个结果。
- **注意：** 如果误拒偏多，补充 `verify.DOC_WORDS` 里的文件类词汇。

### 步骤 3.4 · 规则表与成绩单

- **负责：** C
- **操作：**

```bash
python diagnose.py --template    # 生成两张模板表
python recommend.py --template   # 生成课程库模板
```

然后照模板格式，把培养方案里的学分要求抄进 `requirements.csv`，把开课计划抄进 `courses.csv`，再编一份**模拟成绩单** `transcript.csv`（不要用真实同学的成绩）。
- **产出：** `data/structured/` 下三张 CSV。
- **验收：**

```bash
python diagnose.py --profession 软件工程 --grade 2024
```

能输出各模块的达成率与缺口。

### 步骤 3.5 · 题库构建启动

- **负责：** C（从今天开始，不能等到 9/22）
- **操作：** 按 `eval/questions.csv` 的表头开始出题。事实题逐条从培养方案提取，标准答案必须**人工核对原文后**填写。
- **产出：** 前 60 道事实题。
- **验收：** 每题都能在培养方案里指到具体页码。

---

## 第 4 天 · 9 月 21 日

### 步骤 4.1 · 学分诊断模块

- **负责：** A
- **操作：** 跑通诊断逻辑，确保「不及格课程不计入已修学分」这条规则正确。

```bash
python diagnose.py --profession 软件工程 --grade 2024
```

- **验收：** 造 3 组测试数据（学分充足 / 选修不足 / 有挂科），三组结果都符合预期。

### 步骤 4.2 · 排课建议模块

- **负责：** A

```bash
python recommend.py --semester 2026秋 --profession 软件工程 --grade 2024
```

- **验收：** 推荐结果满足三条：先修课都通过、总学分在 18—24 之间、必修课优先。
- **应急方案：** 如果今天时间不够，**直接砍掉这个模块**。它优先级最低，保住问答和诊断即可。

### 步骤 4.3 · Agent 编排

- **负责：** A
- **操作：** 把三个模块串成一个入口，测试意图识别是否准确。

```bash
python agent.py "软件工程专业毕业最低学分是多少"   # 期望 → search_regulation
python agent.py "我现在修了92学分，还差多少"        # 期望 → diagnose_credits
python agent.py "下学期我该选什么课"                # 期望 → recommend_courses
```

- **验收：** 三个问题路由到三个正确的模块。

### 步骤 4.4 · 前端对接

- **负责：** B
- **操作：** 把 `app.py` 里的假数据换成真实调用（引用 `agent.run`）。
- **产出：** 可交互的完整 Demo。
- **验收：** 在网页上真实提问能拿到真实回答，引用出处能展开。

### 步骤 4.5 · 部署

- **负责：** A
- **操作：** 把服务部署到阿里云函数计算或 ECS，拿到一个公网可访问的地址。
- **验收：** 用手机浏览器打开该地址，功能正常。

---

## 第 5 天 · 9 月 22 日（评测日）

### 步骤 5.1 · 题库就绪检查

- **负责：** C
- **验收：** `eval/questions.csv` 中 `type=fact` 共 100 题、`type=reject` 共 50 题，事实题的标准答案全部填写完毕。

### 步骤 5.2 · 跑四组实验

- **负责：** A 跑脚本，C 核对结果

```bash
python eval/run_eval.py              # 全部四组
python eval/run_eval.py --only fact    # 只跑事实准确率
python eval/run_eval.py --only reject  # 只跑拒答率
python eval/run_eval.py --only ab      # 只跑引用溯源消融对比
```

- **产出：** `eval/results.json`
- **验收：** 四组数据全部有结果，无中断报错。

### 步骤 5.3 · 结果分析与调优

- **负责：** A + C
- **操作：** 看错误样本集中在哪一类问题（数字类 / 课程名类 / 需要推理类），针对性调整 Prompt 或切片。
- **注意：** 调优之后**要重跑评测**，只保留最终一轮的数据作为提交结果。

### 步骤 5.4 · 同学盲评

- **负责：** B
- **操作：** 找 8—10 位同学，每人自由提 5 个真实学业问题，按准确性 / 有用性 / 可信度三项各打 1—5 分。不告诉他们回答来自系统还是人工。
- **产出：** 盲评原始数据 + 平均分。
- **验收：** 至少回收 8 份有效评分。

---

## 第 6 天 · 9 月 23 日

### 步骤 6.1 · 技术说明文档

- **负责：** A + C
- **产出：**
  1. 《模型与算法说明》：RAG 链路、Agent 设计、引用溯源、后验校验机制；
  2. 《环境与运行说明》：即代码目录里的 `README.md`，确认按它能在干净环境跑通；
  3. 《评测报告》：9 月 22 日的四组结果。
- **验收：** 找一个没参与项目的同学，按 README 独立跑通一次。

### 步骤 6.2 · 演示视频

- **负责：** B
- **操作：** 按分工方案里的六段分镜录制，时长控制在 3—5 分钟。
- **产出：** `06-演示/演示视频.mp4`
- **验收：** 完整演示三个模块 + 一个拒答亮点；画面清晰、无隐私信息（成绩单用模拟数据）。

### 步骤 6.3 · 材料汇总

- **负责：** C
- **操作：** 对着阿里赛题的作品输出要求逐项核对：

| 要求 | 对应文件 |
|---|---|
| 模型/算法说明文档 | `05-文档/模型与算法说明.pdf` |
| 可运行代码及依赖说明 | GitHub 仓库链接 + `README.md` |
| 验证集评测结果 | `05-文档/评测报告.pdf` |
| Demo 演示或交互界面 | 演示视频 + 线上地址 |
| 使用阿里云产品 | 百炼（通义千问）+ 向量检索 |

- **验收：** 五项全部齐备，缺一项都不行。

---

## 第 7 天 · 9 月 24 日（提交截止日）

### 步骤 7.1 · 最终检查清单

- **负责：** 全
- **逐项打勾：**
  - [ ] 报名状态为「资格审核通过」
  - [ ] 所有文档能正常打开，无乱码
  - [ ] GitHub 仓库是公开的，链接可用（用无痕窗口验证）
  - [ ] 演示视频能播放，时长符合要求
  - [ ] 线上地址可访问（用手机流量验证，不能只在校园网能开）
  - [ ] 材料中没有真实同学的个人信息
  - [ ] 文件命名规范统一

### 步骤 7.2 · 提交

- **负责：** A
- **操作：** 按大赛系统要求上传全部材料。
- **验收：** 系统显示提交成功，**截图保存**。
- **时间要求：** 上午完成，不要留到晚上。

---

## 附录 A · 每日站会议题模板

每晚 21:30，三个人各说三句：

```
1. 今天我完成了什么（对着手册里的步骤编号说）
2. 明天我做什么（对应到具体步骤编号）
3. 我现在被什么卡住了（没有就说没有）
```

只讲卡点，不讲过程。任何人说「被卡住」时，另外两人必须当场给出支援方案或决定降级方案。

## 附录 B · 降级优先级

进度告急时，按这个顺序砍：

| 优先级 | 模块 | 可否砍 |
|---|---|---|
| P0 | 方案问答 + 引用溯源 | 不可砍，这是作品的核心 |
| P0 | 拒答机制 | 不可砍，这是差异化得分点 |
| P0 | 评测报告 | 不可砍，阿里明确要求 |
| P1 | 学分诊断 | 尽量保，砍了会失去「诊断」卖点 |
| P2 | 选课建议 | 可砍，用文字说明未来规划替代 |
| P2 | 线上部署 | 可砍，用本地录屏代替 |

**原则：宁可交三个完整模块，不交五个半成品。**

## 附录 C · 关键技术命令速查

```bash
# 环境
.venv\Scripts\activate                    # 激活虚拟环境
pip install -r requirements.txt           # 装依赖

# 数据链路（按顺序）
PYTHONIOENCODING=utf-8 python ingest.py      # 1. 切片
python vectorstore.py build                  # 2. 建索引
python vectorstore.py "你的问题"              # 3. 测检索

# 功能模块
python rag.py "你的问题"                     # RAG 问答
python diagnose.py --template                # 生成规则表模板
python diagnose.py --profession 软件工程 --grade 2024
python recommend.py --template               # 生成课程库模板
python recommend.py --semester 2026秋
python agent.py "你的问题"                   # 统一入口

# 前端与评测
streamlit run app.py                         # 启动 Demo
python eval/run_eval.py                      # 跑全部评测
```

---

*本手册由「逐光队」制定，与《选课军师参赛方案书》《逐光队八天分工方案》配套使用。*
